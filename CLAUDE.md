# Project Instructions

## Python Virtual Environment
- Python virtual environment is located in the `venv` directory
- When executing Python scripts, you must use the Python interpreter from the virtual environment
- Windows: use `venv\Scripts\python.exe`
- Linux/Mac: use `venv/bin/python`
